  
<%= search_form_for @search do |f| %>

<div class="panel panel-primary" style="padding:10px" >

        <%= f.label :name_cont, "Nombre" %>
        <%= f.text_field :name_cont %>
        <%= f.label :destination_name, "Destino" %>
        <%= f.text_field :destination_name_cont %>

      <%= f.submit "Buscar", class: "btn btn-large btn-primary" %>
</div>

<% end %>
<%= paginate @models %>
<div class="row">
    <div class="panel panel-primary filterable">
        <div class="panel-heading">
            <h3 class="panel-title">Servicios</h3>
            <div class="pull-right">
            </div>
        </div>
<table class="table table-bordered">
  <thead>
    <tr>
      <th><%= sort_link(@search, :destination_name, "Destino") %></th>
      <th><%= sort_link(@search, :name, "Nombre") %></th>
      <th>Provider aptour</th>
      <th>Operator aptour</th>
      <th colspan="1"></th>
    </tr>
  </thead>

  <tbody>
      <%= render @models %>
  </tbody>
</table>
<%= paginate @models %>
<br>